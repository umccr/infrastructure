name: parallel_cluster
dependencies:
  - python=3.8
  - pip
  - boto3
  - conda-forge::jq
  # FIXME this install awscli v1.18
  # So cannot login to AWS via sso
  # When in parallel_cluster environment
  - conda-forge::awscli
  - pip:
    - aws-parallelcluster >= 2.8.0
