name: parallel_cluster
dependencies:
  - python=3.7
  - pip
  - conda-forge::jq
  - conda-forge::awscli
  - pip:
    - aws-parallelcluster == 2.5.0 # This version is buggy and failes to create worker nodes
