This is a terraform module Skeleton, use me to create new modules and stacks for UMCCR!

Assuming that `FOO` is the name of your new module/app:

```
cp -a skel FOO && cd FOO
sed -i 's/YOUR_MODULE/FOO/g' *
```

And off you go!